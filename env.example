# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google OAuth Configuration (Required for Google Authentication)
# Configure these in your Supabase dashboard under Authentication > Providers > Google
# 
# 1. Supabase Callback URL (used internally by Supabase):
# - https://hvhwqtebimabyrcgprwd.supabase.co/auth/v1/callback
#
# 2. Redirect URLs to add in Google Cloud Console:
# - http://localhost:3000/auth/callback (for development)
# - https://your-domain.com/auth/callback (for production)
#
# 3. In Supabase dashboard, add your app's callback URLs:
# - http://localhost:3000/auth/callback (for development)
# - https://your-domain.com/auth/callback (for production)

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret for API
JWT_SECRET=your_jwt_secret_key

# Supabase Service Role Key (for server-side operations)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Optional: External Service Keys
GOOGLE_MAPS_API_KEY=your_google_maps_key
SENDGRID_API_KEY=your_sendgrid_key 